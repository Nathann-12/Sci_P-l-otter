# CLAUDE.md — คู่มือสำหรับ AI ที่ทำงานกับโปรเจคนี้

> อ่านไฟล์นี้ก่อนเริ่มงานทุกครั้ง แล้วทำตามกติกาในนี้อย่างเคร่งครัด

## โปรเจคนี้คืออะไร
**SciPlotter** = เครื่องมือเดสก์ท็อปวิเคราะห์/พล็อตข้อมูลวิทยาศาสตร์ (PySide6 + Matplotlib + pandas/numpy/scipy)
เป้าหมายระยะยาว: เป็น **Research OS สำหรับนักวิจัยไทย** — เปิดข้อมูล → วิเคราะห์ → ทำกราฟ → โมดูลเฉพาะทาง → เขียนรายงาน → reproduce ได้
ขอบเขตฟีเจอร์ทั้งหมด + สถานะปัจจุบัน อยู่ใน **[ROADMAP.md](ROADMAP.md)** (แหล่งความจริงเดียว)

## วิธีรันและทดสอบ (สำคัญ — ใช้ interpreter ตัวนี้เท่านั้น)
- venv ของโปรเจคคือ `.venv/Scripts/python.exe` (Python 3.11.9) — **อย่าใช้ `py`/`python` ระบบ** (เคยพังเพราะ venv เก่าชี้ interpreter ที่ถูกลบ; ดู `memory/env-and-run.md` ฝั่งผู้ใช้)
- **รันเทสต์ (headless):**
  ```bash
  QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -m pytest tests/ -q
  ```
  Baseline ปัจจุบัน (ตรวจล่าสุด 2026-07-11): **760 passed, 3 skipped** in ~600s
- **เปิดแอป (GUI):** `.venv/Scripts/pythonw.exe main.py` (หรือ `python main.py`)
- **smoke import:** `QT_QPA_PLATFORM=offscreen .venv/Scripts/python.exe -c "import main; main.MainWindow"`
- ถ้า dependency หาย: `.venv/Scripts/python.exe -m pip install -r requirements.txt`

## สถาปัตยกรรม
`main.py` (~675 บรรทัด) เป็น **thin shell**: imports/setup, `MainWindow.__init__` (ประกอบ UI: shell + MDI workspace + Project Explorer side tabs + docks), การ wire สัญญาณ, helper เล็ก (`_show_status`/`_icon`/`show_about`/`resizeEvent`/`_refresh_workbook`), `main()`
`MainWindow` ประกอบจาก **mixin 20 ตัว** (logic อยู่ใน mixin ไม่ใช่ใน main.py) — รวม `main_window_view_access_mixin` (view seam: `notify`/`ask_choice`/`selected_x_column`/`active_axes`):

| Mixin | หน้าที่ |
|---|---|
| `main_window_data_mixin` | โหลดไฟล์, คอลัมน์, datetime/numeric, `_get_xy` |
| `main_window_plot_mixin` | คำสั่งพล็อต (line/scatter/histogram/bar/overlay) |
| `main_window_plotcore_mixin` | เครื่องวาด: `apply_plot`, axes/canvas, theme, `change_plot_style`, `refresh_plot` |
| `main_window_fit_mixin` | curve fitting |
| `main_window_export_mixin` | export CSV/PNG/FFT |
| `main_window_session_mixin` | staging, save/restore session, closeEvent |
| `main_window_spectrogram_mixin` | spectrogram dialog + handlers |
| `main_window_view_mixin` | zoom/crosshair/clear/tab view |
| `main_window_menu_mixin` | สร้างเมนู (`_init_menu`) + docks/managers |
| `main_window_toolbar_mixin` | สร้าง toolbar + styling + responsive |
| `main_window_panels_mixin` | left panel + inspector tabs + layer manager |
| `main_window_analysis_mixin` | cross-correlation + peak detection |
| `main_window_equation_mixin` | พล็อตจากสมการ |
| `main_window_settings_mixin` | config load/save + Settings dialog |
| `main_window_features_mixin` | feature_add_*, FFT, report, units, derived column |
| `main_window_actions_mixin` | dispatcher (on_action_*), dataframe accessors, drag&drop |
| `main_window_gassensor_mixin` | โมดูล Gas Sensor (rail context + เมนู): response/t90, ตรวจรอบแก๊ส, calibration+LOD, dilution — logic ใน `analysis/gas_sensor.py` |
| `main_window_electrochemistry_mixin` | Electrochemistry module (rail context + menu): CV peak metrics, Randles-Sevcik/ECSA, Tafel, GCD supercapacitor metrics, EIS Rs/Rct/Nyquist/Bode — logic in `analysis/electrochemistry.py` |
| `main_window_spectroscopy_mixin` | Spectroscopy module (Modules Gallery + Modules menu): baseline/normalize, peak table/FWHM/area, Raman D/G, Tauc band gap, XRD Scherrer — logic in `analysis/spectroscopy.py` |
| `main_window_materials_mixin` | Materials Science module (Modules Gallery + Modules menu): I-V conductivity/resistivity/sheet resistance, Arrhenius activation energy, TGA/DSC thermal metrics, sample ranking — logic in `analysis/materials.py` |
| `main_window_physics_mixin` | Physics / General Lab module (Modules Gallery + Modules menu): Ohm's law, RC time constant, pendulum gravity, uncertainty propagation — logic in `analysis/physics_lab.py` |
| `main_window_workflow_mixin` | Reproducibility (เมนู Tools): `_record_op` + history/export/import/re-run/gen สคริปต์ — logic ใน `core/history.py` (ops ใหม่ต้องมี entry ใน `REPLAY_REGISTRY` + `_SCRIPT_TEMPLATES`) |
| `main_window_plotstyle_mixin` | ตกแต่งกราฟแบบ Origin "Plot Details" (View→Format Graph / Ctrl+Shift+F / ดับเบิลคลิกกราฟ): เปิด `dialogs/plot_details_dialog.py` แก้ style ผ่าน `core/plot_style.py` (axes/grid/legend/figure + per-curve) |

**ชั้น UI (โมเดล OriginPro เต็มรูปแบบ — สำคัญ)**
- **UX ทั้งแอปยึด loop แบบ Origin** (ดู [docs/UX_FLOW.md](docs/UX_FLOW.md) — บังคับสำหรับฟีเจอร์ใหม่): เปิดไฟล์/พิมพ์ใน Book → เลือกคอลัมน์บนชีต → คลิกไอคอนพล็อตใน function bar สองแถวด้านบนหรือปุ่มบนชีต → **Graph window ใหม่เสมอ**; ไม่มีแผง workflow ซ้ายแบบถาวร — Worksheet + two-row function toolbar + Project Explorer แบบ parked side tab + เมนู เท่านั้น; **ห้ามซ่อนความสามารถหลักไว้ใน dock/panel ที่ปิดอยู่**
- **Multi-book แบบ Origin**: 1 ไฟล์ = 1 Book (เปิด/ลากไฟล์ → `_stage_insert` → `_open_book_for_dataset`); คลิกหน้าต่าง Book = สลับข้อมูล (`MdiWorkspace.bookActivated` → `_on_book_activated` ตั้ง `self.workbook`/`self._df`); DataFrame ของแต่ละ Book อยู่ที่ `WorkbookWidget.source_df` + registry `_datasets` (session ใช้ตัวนี้)
- **workspace กลาง = MDI** ([UI/mdi_workspace.py](UI/mdi_workspace.py) `MdiWorkspace`) — Book/Graph เป็น QMdiSubWindow ลอยได้แบบ Origin. `MdiWorkspace` **เลียนแบบ API ของ TabManager** และถูกตั้งเป็น `self.tabs` → โค้ดพล็อต (plot/plotcore/view mixin) reuse ได้โดยไม่ต้องแก้. MainWindow เปิดแบบ **sheet-first** (`start_with_graph=False`): มี Book1 อย่างเดียว, กราฟใหม่ = `self.tabs.add_tab()` ตอนผู้ใช้สั่ง plot; worksheet = `add_book()`. **อย่าเขียนโค้ดพล็อตให้ผูกกับ QTabWidget** — ใช้ API ของ `self.tabs`
- **worksheet แบบ Excel** ([widgets/workbook.py](widgets/workbook.py) `WorkbookWidget`) — แถว Long Name/Units/Comments/F(x)=; **หัวคอลัมน์ถือ designation แบบ Origin** (`A(X)`/`B(Y)`/`C`=Disregard, คลิกขวาหัวคอลัมน์ → Set As, โหลดไฟล์แล้วคอลัมน์เวลาเป็น X อัตโนมัติ); แถบเครื่องมือ/คลิกขวาบนชีต emit `use_data_requested`/`plot_requested`/`overlay_requested` → `adopt_workbook_data` (data mixin) / `plot_from_workbook(style, new_graph)` (plot mixin — เส้นทางพล็อตจากชีตทั้งหมดรวมที่นี่)
- **Data workflow menu contract** (`main_window_menu_mixin.py`) — top menu `Data` เป็น workflow menu ไม่ใช่ legacy flat menu: `Active Book` (Open/Batch Import/Use Active Worksheet Data/Reload Column List), `Columns` (Create Derived Column `Ctrl+Shift+D`, Set Column Types, Add Row/Column), `Units + Metadata`, `Quick Transforms`, `Clean Data`. คำสั่งที่แตะ worksheet ต้องใช้ active Book/worksheet และต้องมี QAction user-flow tests เหมือน Process/Analysis; ห้ามย้ายกลับไปเป็น dropdown 2 action แบบเดิม
- **Process workflow menu contract** (`main_window_menu_mixin.py`) — top menu `Process` ต้องเป็น grouped workflow menu ไม่ใช่ flat legacy list: `Quick Actions`, `Frequency & Spectrum`, `Smoothing & Filters`, `Signal Transforms`, `Correlation & Convolution`, `Clean & Prepare Data`, `Summarize & Aggregate`. Top-level ของ Process ห้ามมีกอง action ตรง ๆ; ทุกคำสั่งต้องมี icon/status tip เท่าที่ทำได้และมี QAction user-flow tests ผ่าน path หมวดจริง
- **single-column plot contract** — selection ของผู้ใช้ต้องชนะ fallback อัตโนมัติ: เลือกคอลัมน์เดียวใด ๆ ให้ plot คอลัมน์นั้นเป็น `Y` เทียบ `Row` 1..N แบบ Excel; X/Y designation ใช้เมื่อเลือกหลายคอลัมน์หรือไม่ได้เลือกคอลัมน์; histogram ใช้คอลัมน์ที่เลือกตรง ๆ แม้คอลัมน์นั้นเป็น `X`
- **Charts mega menu** (`widgets/chart_mega_menu.py`) — เมนู `Charts` บน menubar แบบ OriginPro: sidebar หมวด + lazy thumbnail grid + Recently Used; thumbnail ใช้ deterministic example data เพื่อให้มีภาพแม้ Book ว่าง แต่ตอนคลิกพล็อตใช้ active Book จริง; basic line/scatter/bar/hist วิ่งผ่าน worksheet designation/new Graph ส่วน registry/basic advanced plots เปิด `dialogs/plot_data_mapping_dialog.py` ให้เลือก Primary/X, Y series, Z และ Group ก่อนสร้าง Graph (Cancel ต้องไม่สร้าง Graph เปล่า)
- **Two-row function toolbar contract** (`main_window_toolbar_mixin.py::_create_toolbar_actions` + `styles/toolbar.qss`) — top toolbar เป็น 2 แถวแบบ Origin (`FunctionToolbarPrimary` + `FunctionToolbarSecondary`) แต่ **เก็บเฉพาะ essentials ให้โล่ง**: Row 1 = File/Data/View, Row 2 = Plot (รวม ~30 icon เท่านั้น ไม่ใช่ ~90); เครื่องมือเฉพาะทาง (process/clean/signal/analysis/peak/annotation/dataset/workflow) ย้ายไป side/bottom docks. icon-only 16px, ทุก non-separator action บน top ต้องมี `toolbarIconKey` ไม่ซ้ำกัน; **ทุก action ไม่ว่าอยู่ surface ไหนต้องอยู่ใน `toolbar_actions` ด้วย key เดิม** (เทสต์/command surface เรียกผ่าน dict key) — ห้ามเปลี่ยน key เดิมตอนย้าย surface; basic plot shortcuts ยังมี `plot_bar_actions` (6 keys) + `plot_toolbar` alias = `self.tb`. Mapping ไอคอนอยู่ที่ `_QTA_ICON_MAP` ใน `main.py` (qtawesome MDI)
- **Side/bottom tool docks contract** (`main_window_toolbar_mixin.py::build_side_toolbars` + `_create_dock_tool_groups`, เรียกใน `main.py` หลัง `_init_menu`) — Origin-style docks จัดหมวด: **Left** `GraphToolsToolbar` = graph interaction (crosshair/box-zoom/zoom-in-out/reset/format) + annotation (ann_*) + undo/redo; **Right** `WindowsToolbar` = windows (addtab/window_tile/window_cascade/inspector) + export (export_figure/export_data/batch_export/copy_graph) + Book ops (dataset_*); **Bottom** `PlotAnalyzeToolbar` = process/clean/signal/analysis/cc/peak/workflow. ทุก action ต้องต่อ handler จริง (ห้ามปุ่มตาย); checkable ที่ top bar มีแล้ว (crosshair/boxzoom/reset_view/format_graph/inspector) **reuse ตัวเดิม** ผ่าน `_reuse_action` เพื่อ sync state; group ที่ย้ายมาจาก Row 2 เดิม **คง key เดิมทุกตัว** (moving_average/fill_missing/stats/ann_text/dataset_group/workflow_history ฯลฯ) — dict-key user-flow tests จึงไม่พัง; top-bar uniqueness contract ต้องไม่พัง (side dock key ที่เพิ่มใหม่ใช้ prefix `left_/right_` เช่น `left_zoom_in`)
- **Graph-scoped toolbar contract** — คำสั่ง toolbar ที่แตะกราฟ (`Plot`, `Spectrogram`, `Export Figure/Data`, `Crosshair`, `Box Zoom`, `Format Graph`, `Plot from Equation`, `Processors/FFT`) ต้อง resolve ไปที่ Graph window ที่เลือกอยู่ หรือ Graph ล่าสุดที่ผู้ใช้เลือกเมื่อ Book โฟกัสอยู่; ห้าม fallback ไป Graph 1 ตามตำแหน่งโดยไม่ตั้งใจ
- **Plot Details decoration contract** (`dialogs/plot_details_dialog.py` + `core/plot_style.py`) — live Apply/Preset ห้ามเปลี่ยน live figure size/DPI; print size/DPI เก็บใน `GraphTab._print_figure` เพื่อ export เท่านั้น. หลัง apply ต้อง **relayout ด้วย `tight_layout` เท่านั้น ห้าม `set_size_inches` บน live canvas** — การ force ขนาดจาก Qt *logical* pixels ทำให้ figure เล็กกว่า widget เท่า devicePixelRatio บนจอ HiDPI (Windows 125%/150%) = บั๊ก "กราฟย่อเวลาตกแต่ง" (`_relayout_live_figure` ใน `main_window_plotstyle_mixin.py`). Decoration engine ครอบคลุมระดับ Origin: title/label typography (color/bold/italic/family/pad), ticks (length/width/color/label-color/rotation/minor/mirror/sci-notation), tick-label display override (`tick_labels`: decimal/scientific/engineering/percent, decimal places, divide factor, **formula เช่น `2*x` ผ่าน `compile_tick_formula` — AST whitelist เท่านั้น ห้ามแทนด้วย eval ตรง และ formula ชนะ divide factor**, prefix/suffix, plus/minus, thousands), แท็บ Scale แบบ Origin (From/To/Type/Reverse ต่อแกน, rescale margin % ตอน autoscale, Major "By Increment" + **Anchor Tick** ผ่าน `MultipleLocator(offset)`, Minor "By Increment" หรือ "By Counts" — increment ชนะ counts), per-side spine visibility, reference lines (gid `_ps_refline_*` — re-apply ต้องไม่ stack, มี label/width/opacity), grid major+minor แยก color/style/width/alpha + axis, legend title/spacing/markerscale/pads, per-line marker face/edge/width + fillstyle/drawstyle/zorder + glow/shadow. `read_style`/`apply_style` เป็น schema กลางที่ dialog ยึด (เพิ่ม key ต้องมี default ใน `read_style` เพื่อ seed control). **Diff-apply stability contract:** dialog เก็บ baseline (`_seed_style`/`_seed_line_styles` = get_style() ตอนสร้าง) และ `_apply_plot_details` apply เฉพาะ `diff_style(baseline, current)` แล้ว re-baseline หลัง apply — **identity Apply ต้องเป็น visual no-op** (control ที่ไม่ถูกแตะห้ามแตะกราฟ แม้ seed อ่านมาเพี้ยน); key ที่ต้องไปด้วยกันดู `_AXES_KEY_GROUPS`, section `grid/legend/tick_labels/effects` diff ทั้งก้อน; ปิด tick-label override ต้องคืน ScalarFormatter เฉพาะแกนที่ถือ formatter ของเรา (tag `_ps_tick_label_fmt`)
- **Function-menu action contract** — เมนู `Process`, `Analysis`, `Plot`, `Export`, `Tools` ต้องมี user-flow tests ที่ trigger ผ่าน `QAction` จริง ไม่เรียก method ตรงเท่านั้น; action ที่แตะข้อมูลต้องใช้ active Book/worksheet และ action ที่แตะกราฟต้องใช้ selected/last-selected Graph ตาม graph-scoped contract
- **Analysis menu Origin contract** (`main_window_menu_mixin.py`) — เมนู `Analysis` ต้องมีหมวดแบบ Origin: `Statistics`, `Mathematics`, `Data Manipulation`, `Fitting`, `Signal Processing`, `Peaks and Baseline` พร้อม mirror ฟังก์ชันที่มี backend จริงจาก Process/Data/Fit/Peak; signal actions เช่น Smooth/FFT/STFT/Wavelet(CWT dialog)/Correlation/Hilbert/Envelope/Decimation ต้อง trigger QAction ได้จริง ห้ามใส่เมนู placeholder ที่กดแล้วไม่ทำอะไร
- **Activity rail contract** (`widgets/activity_rail.py` + `styles/shell.qss`) — rail ด้านซ้ายเป็น scalable module dock: fixed width 76px, header `SP`, section `MODULES`, scrollable module-card list และ future slot; module ใหม่ต้องลงผ่าน `AppShell.register_context(...)` เท่านั้น แต่ **register แล้วห้ามโชว์เองตอน startup**; เปิด module UI แบบ explicit ด้วย `AppShell.show_activity_context(...)`; ห้ามยัด workflow หลักของ worksheet/plot เข้า rail
- **Parked side-panel contract** (`widgets/side_panel_tabs.py` + `AppShell.add_side_panel`) — Project Explorer, Messages Log และ Smart Hint Log อยู่ใน vertical side tabs ซ้ายสุดแบบ collapsed-by-default; collapsed width = 24px, คลิก tab เพื่อเปิด/คลิก tab เดิมเพื่อเก็บ; bottom `dock_tabs` เป็น compatibility surface และต้องซ่อนอยู่จนกว่าจะมี caller ใช้ `AppShell.add_dock(...)`
- **Settings dialog contract** (`dialogs_settings.py` + `settings.py` + `styles/theme.py`) — dialog ต้อง compact ไม่ล้นจอ (`900x620`, min `720x480`) และใช้ scroll ต่อ tab; ค่าใน Settings ต้องมีผลจริง: **Theme เลือกผ่าน `theme_mode` (`dark`/`light`/`custom`) ไม่ใช่ผ่าน qss path** — Built-in Dark/Light = qdarktheme dark/light mode, Custom QSS = explicit path (custom ที่หา path ไม่เจอ fallback เป็น dark); **Accent Color + Background Color เลือกสีใดก็ได้** (`accent_color`/`background_color` ใน `AppearanceConfig`) แล้ว `build_theme_palette()` คำนวณ surface/border/text/contrast + light/dark auto จากพื้นหลัง, background ว่าง = ใช้ default ของ mode; Apply to Matplotlib ต้อง sync font+size ไป `matplotlib.rcParams`, Plot Behavior บันทึก `QSettings("plot/mode")`; ห้ามเพิ่ม option ที่เป็น UI-only โดยไม่มี runtime/persistence/validation
- **Theme runtime contract** (`styles/theme.py`) — ทั้งแอปใช้เส้นทางเดียว: `apply_theme_from_config()` → `apply_font()` + `apply_qss(theme_mode/accent/background/font)`; `apply_qss` เก็บสีปัจจุบันไว้ที่ app properties (`sciplotterThemeMode`/`sciplotterAccentColor`/`sciplotterBackgroundColor`) เพื่อให้ late-created popup/icon ตามธีมได้. `_ApplicationThemeRuntime` เป็น app-level event filter (Show/Polish/StyleChange) ที่ retint component QSS (`render_theme_qss` — alias hex เดิม→palette), rebase QPalette+font ให้ dialog/popup ที่สร้างทีหลัง และเรียก hook `apply_application_theme()` ของ widget ที่ประกาศไว้ (MdiWorkspace/WorkbookWidget); widget ที่ไม่อยากโดนธีมตั้ง property `sciplotterThemeBypass=True`. **ไอคอน**: qtawesome ทุกตัววิ่งผ่าน `themed_icon()` (tint monochrome ตาม mode/state, cache key ผูกกับสี), `refresh_application_icons()` recolor เฉพาะ icon ที่เป็น monochrome ของเรา — icon สี (chart thumbnail/ColorButton) ต้องตั้ง `preserveIconColors=True` เพื่อกันโดน retint. `main()` ตั้ง `AA_DontUseNativeDialogs`+`AA_DontUseNativeMenuWindows` เพื่อให้ธีมครอบคลุมทุก dialog/menu
- **Projection seam** (`main_window_plotcore_mixin.py`) — `apply_plot(..., projection="polar"|"3d"|None)` และ `get_main_axes(..., projection=...)` สลับ axes projection โดยไม่ปล่อย `canvas.ax` ชี้ axes เก่าที่ถูก `fig.clf()` แล้ว; registry plot ที่ต้องใช้แกนพิเศษให้ใส่ `projection` (เช่น polar) หรือ `is3d=True`
- **Contour/Heatmap data contract** (`plots/surface_plots.py`) — Filled Contour/Contour Lines ใช้ 3 numeric columns แรกเป็น XYZ และ triangulate โดยตรง; Heatmap ใช้ numeric columns ทั้ง Book เป็น matrix (แถวภาพ = คอลัมน์, แกน X = observation); ข้อมูลไม่พอ/collinear แสดง placeholder ไม่ throw
- **Multi-Column/Multi-Panel data contract** (`plots/multicolumn_plots.py`) — numeric column แรกเป็น X และคอลัมน์ถัดไปเป็น Y series; ถ้ามี numeric column เดียวใช้ row number เป็น X; `waterfall_3d` ลงทะเบียน `is3d=True`; `subplot_grid` ลงทะเบียน `multi=True` และ `_draw_multi_panel()` ต้องตั้ง `canvas.ax = fig.axes[0]` หลังสร้าง axes ใหม่
- **Polar data contract** (`plots/polar_plots.py`) — 1 numeric column = radius กับมุมกระจายเท่ากันรอบวง; 2+ numeric columns = column แรกเป็น angle และคอลัมน์ถัดไปเป็น radius series; angle ที่มีค่าสูงกว่า `2π` จะตีความเป็น degree; registry plot ต้องใส่ `projection="polar"`
- **Signal/Frequency data contract** (`plots/frequency_plots.py`) — Phase Plot ใช้สอง numeric columns หรือ successive samples ถ้ามีคอลัมน์เดียว; Nyquist ใช้ `real/re` + `imag/im` aliases และแกน Y เป็น imaginary จริงไม่กลับเครื่องหมาย; Bode ใช้ `frequency/freq` + `magnitude/mag` + `phase` หรือ derive magnitude/phase จาก `frequency + real + imag`, กรอง positive frequency และ sort แบบ stable
- **Signal transforms contract** (`analysis/signal_filters.py` + `main_window_features_mixin.py`) — Process menu แยกตามงานจริง: `Frequency & Spectrum` มี FFT/PSD/STFT/IFFT/Harmonic Analysis, `Smoothing & Filters` มี Moving Average/Butterworth/Smooth/Window/Decimation, `Signal Transforms` มี Hilbert/Envelope/Instantaneous Frequency/Zero Padding, และ `Correlation & Convolution` มี Auto-correlation/Convolution/Deconvolution; transform ที่ความยาวเท่า Book ปัจจุบันเพิ่มเป็นคอลัมน์ใหม่และ sync worksheet โดยรักษา Origin designation เดิม, ส่วน output ที่ความยาวเปลี่ยนหรือเป็น matrix/long-form เปิด Book ผลลัพธ์ใหม่
- **Batch figure export contract** (`main_window_export_mixin.py` + `core/export_request.py`) — Export → Batch Export Graphs เซฟทุก Graph window ที่มี plot content ไป folder เดียว โดย default ข้าม Graph เปล่า; ใช้ options เดียวกับ Export Figure (`format`, `dpi`, transparent, tight bbox), ตั้งชื่อไฟล์ `01_<Graph title>.<ext>` ด้วย `safe_export_stem()` และยังเคารพ print size จาก Plot Details ต่อ Graph
- **Broken axis contract** (`core/plot_extras.py` + `main_window_plotextra_mixin.py`) — Plot → Broken Axis split active line graph เท่านั้นในรอบนี้; helper redraw line artists ลง axes คู่ (X หรือ Y break), ซ่อนช่วง `lower..upper`, ใส่ diagonal break marks และอัปเดต `tab.canvas.ax` ไปยัง axes ใหม่หลัง `fig.clf()`; ถ้าต้องรองรับ scatter/bar/image ต้องเพิ่ม copy logic แยกพร้อม tests
- **hidden compatibility state (หนี้เทคนิคที่เหลือ)**: `_panel_left` + `CompactPlotPanel` ยังถูกสร้างแบบซ่อนเพื่อรักษา column selection (`cbX/cbY`) และ legacy actions ที่ analysis/session บางส่วนยังใช้; style aliases `spLineWidth/chkMarker` และ histogram aliases ถูกถอดแล้ว โดย style ใช้ `PlotOptions`; activity rail/context ต้องซ่อนตอน startup แม้มี context ลงทะเบียนแล้ว
- **Project Explorer** ([UI/project_explorer.py](UI/project_explorer.py)) — parked side tab ซ้าย ต้นไม้ Book/Graph, sync ผ่าน signal `subWindowAdded/Removed/Renamed` ของ `MdiWorkspace`
- **shell/docks** — `UI/shell/app_shell.py` (activity rail + parked side panels + command palette Ctrl+K), `UI/docks/` (AI/Log), `UI/welcome.py`, `widgets/activity_rail.py`, `widgets/side_panel_tabs.py`, `widgets/command_palette.py`
- **ธีม** — ฐานคือ **qdarktheme** (ตั้งใน `styles/theme.py::apply_theme_from_config`) + override `shell.qss`/`sidepanel.qss`/`toolbar.qss`; ไอคอนใช้ **qtawesome** ผ่าน `_icon()` (map ที่ `_QTA_ICON_MAP` ใน main.py). **สีทั้งแอปมี source of truth เดียว** = `styles/theme.py::DARK_CUSTOM_COLORS` (accent `#4F9CF9`, bg `#1e2126`, border `#3a3f44`) — QSS ทุกไฟล์/สีฝังใน widget ต้องอยู่ family นี้; title bar ของ QMdiSubWindow คุมด้วย `selection-background-color` ใน `_MDI_STYLESHEET` (ไม่ใช่ QPalette เพราะ QStyleSheetStyle ทับ)
- **view-access seam** ([main_window_view_access_mixin.py](main_window_view_access_mixin.py)) — logic เรียก `self.notify/ask_choice/selected_x_column/active_axes` แทนแตะ widget ตรง (ทำให้สลับ UI ได้)

**โมดูล/แพ็กเกจอื่น**
- `core/` — `plot_data` (เตรียมข้อมูลพล็อต), `plot_request` (`PlotRequest`/`PlotOptions`/histogram/bar requests), `export_request` (visible-range/figure export models + pure extraction), `plot_mode`, `session`, `units`, `logging_setup`
- `widgets/` — `plot_tabs` (PlotCanvas/GraphTab/TabManager/CompactPlotPanel), `workbook`, `layer_manager`, `color_button`, `mpl_preview`, `activity_rail`, `command_palette`
- `dialogs/` + `dialogs_*.py` — กล่องโต้ตอบต่าง ๆ
- `processors.py` (สัญญาณ/feature), `loaders.py` + `file_io.py` (อ่านไฟล์), `analysis/fitting.py` (โมเดล fit)
- `read_mms_cdf.py` (อวกาศ/CDF), `peaks.py`, `crosscorr.py`, `eqplot.py`/`eqplot3d.py`, `annotations.py`, `report_generator.py`, `three_d_view.py`, `charts_gallery.py`

## กติกาการทำงาน (บังคับ)
1. **เสร็จแล้วต้องอัปเดต 2 ที่เสมอ:** (ก) ติ๊กสถานะใน [ROADMAP.md](ROADMAP.md) เป็น `✅` (ข) เพิ่ม/อัปเดตเทสต์ใน `tests/`
2. **เทสต์ต้องเขียวก่อนถือว่าเสร็จ** — รันชุด headless ข้างบน อย่าทิ้งให้แดง
3. **อย่าทำให้ main.py อ้วนอีก** — logic ใหม่ไปอยู่ mixin/โมดูลที่เหมาะสม; main.py เป็น shell เท่านั้น
4. **ห้าม bare `except:`** และเลี่ยง `except Exception: pass` แบบเงียบ — ให้ `logger.debug(..., exc_info=True)` อย่างน้อย
5. **refactor = pure move** — ห้ามแก้พฤติกรรมเงียบ ๆ ระหว่างย้ายโค้ด
6. เทสต์ปัจจุบันส่วนใหญ่เป็น **structure test** (เช็คการต่อสาย) — ฟีเจอร์ใหม่ต้องมี **behavioral test** (เช็คผลลัพธ์จริง) ด้วย
7. commit ทีละหน่วยงานที่มีความหมาย; **อย่า push** เองเว้นแต่ผู้ใช้สั่งชัดเจน (push เข้า `main` ถูก guard ไว้)

## สูตรเพิ่มฟีเจอร์ (ทำตามแพทเทิร์นเดิม)
ฟีเจอร์ใหม่ปกติ = 4 ชิ้น:
1. **logic** — ฟังก์ชันคำนวณบริสุทธิ์ใน `processors.py`/`analysis/`/`core/` (unit-test ได้)
2. **dialog** (ถ้ามี UI input) ใน `dialogs/`
3. **method ใน mixin** ที่เหมาะ (หรือสร้าง mixin ใหม่ถ้าเป็นโดเมนใหม่ เช่นโมดูลเฉพาะทาง) — ทำหน้าที่ต่อ UI เข้ากับ logic
4. **wire action** ในเมนู/toolbar (`main_window_menu_mixin` / `main_window_toolbar_mixin`)
5. **test** ใน `tests/` + ติ๊ก ROADMAP

> โมดูลเฉพาะทางใหม่ (Gas Sensor, Electrochemistry, ฯลฯ) ควรเป็น mixin/แพ็กเกจของตัวเอง + logic แยกเป็นโมดูลบริสุทธิ์เพื่อ test ง่าย

## หนี้ทางเทคนิคที่ควรรู้ (ก่อนรื้อใหญ่)
- analysis/feature logic บางส่วนยังผูกกับ Qt widget โดยตรง (เช่น `self.cbX.currentText()`, `QInputDialog`, `self.statusBar()`) → ถ้าจะ "เขียน UI ใหม่" ต้อง decouple ส่วนที่เหลือก่อน
- behavioral test ยังบาง — เป็นความเสี่ยงหลักเวลารื้อใหญ่
- `except Exception: pass` ยังเหลือเยอะนอกโซน data-loading

## Current Debt Snapshot (2026-07-06)
- **P1 — state seam ของ MainWindow ยังเปราะ:** state หลักกระจายอยู่ที่ `_df`, `_datasets`, `workbook`, `tabs`, hidden widget aliases และ signal bridge ระหว่าง MDI/Workbook/mixins; แก้ flow import→activate→plot ผิดจุดมีโอกาสกระทบหลายฟีเจอร์พร้อมกัน
- **P1 — specialized analysis ยังแตะ widget state:** core line/scatter/overlay/histogram/bar/export ใช้ request/options models แล้ว แต่ analysis/features บางส่วนยังอ่าน `cbX/cbY/statusBar()` ตรง; ย้ายผ่าน view-access seam ก่อนลบ compatibility panel
- **P1 — hidden compatibility layer เหลือเฉพาะ consumer จริง:** `_panel_left` และ `CompactPlotPanel` ยังถือ `cbX/cbY` กับ legacy actions; style/histogram aliases และ dead inspector block ถูกลบแล้ว ห้ามลบ panel ที่เหลือจนกว่า analysis/session จะย้ายครบ
- **P2 — Plot Gallery ยังเป็น seam ใหม่:** `plots/registry.py` + `dialogs/plot_gallery_dialog.py` + `main_window_gallery_mixin.py` ผ่านเทสต์แล้ว และมี `PlotDataMappingDialog` กันกรณี registry auto-pick column ผิด แต่ยังถือเป็น integration surface ใหม่ที่ควรเฝ้าดูเวลาแก้ menu/toolbar/MDI plotting
- **P2 — Gallery UX polish backlog:** lazy thumbnails ใช้งานได้แล้ว แต่ plot ที่สร้าง subplot grid หนัก (เช่น `scatter_matrix`/`subplot_grid`) อาจควร cache หรือใช้ placeholder ถ้าเจอ lag; ชื่อ `Heatmap` ในหมวด Relational กับ `Contour, Heatmap` คนละชนิดแต่คล้ายกัน อาจต้อง rename label ให้ชัดขึ้นภายหลัง
- **P3 — เอกสาร/สถานะจริงเคยไม่ sync:** baseline test count ในไฟล์นี้เคยล้าหลังสถานะจริง; ถ้าทำงานที่เปลี่ยน flow หรือ baseline แล้ว ให้ update CLAUDE/ROADMAP พร้อมกัน

## Recommended Refactor Order
1. ย้าย analysis/features ที่ยังอ่าน `cbX/cbY` ตรงให้ใช้ view-access/data-selection seam
2. เมื่อ consumer เหลือศูนย์ ค่อยแทน hidden `CompactPlotPanel` ด้วย selection state ที่ไม่ผูก Qt widget
3. เก็บ `except Exception: pass` ในเส้นทาง plotting/view ก่อนรื้อ UI รอบใหญ่

## Recently Completed
- **2026-07-06 — active dataframe seam:** รวม precedence ไว้ที่ `MainWindowActionsMixin._resolve_active_dataframe()` (`_df` → compatibility `current_df` → Workbook source → selected/first staged dataset), ลบ unreachable fallback และให้ Gallery/column refresh ใช้ resolver เดียวกัน
- ป้องกัน stale state เมื่อสลับไป Book ว่างโดย clear `_df`/`_current_path`; มี regression tests ครอบคลุม precedence, selected dataset, no-data และ empty-book activation
- **2026-07-06 — PlotRequest seam:** เพิ่ม immutable `core.plot_request.PlotRequest`; line/scatter/overlay รับ request โดยตรง และ multi-Y workbook ไม่ต้องสลับ `cbY` ระหว่างสร้างแต่ละ series
- **2026-07-06 — clean datetime coercion:** `prepare_plot_data()` ใช้ pandas mixed-format parsing แบบ explicit; categorical/mixed strings ไม่ปล่อย infer-format warning แล้ว และ full suite เหลือ 0 warnings
- **2026-07-06 — PlotOptions + export requests:** line/scatter/overlay/histogram/bar ใช้ immutable `PlotOptions`/request models; visible-range export แยกเป็น pure extractor และ advanced figure export ใช้ `FigureExportOptions`
- ถอด hidden style/histogram aliases (`spLineWidth`, `chkMarker`, `cbHist`, bins/fit controls), ลบ inspector block ที่ unreachable หลัง `return` และคง Origin loop/column designation behavior ด้วย behavioral tests
- **2026-07-06 — top Charts mega menu:** แทนกริดข้อความ 2×4 เดิมด้วย Origin-style categorized menu ขนาดใหญ่, lazy example thumbnails, Recently Used และ routing active Book ไป new Graph; ถอด `_cg_get_fig`/gallery wiring เก่าที่ซ้ำกัน
- **2026-07-06 — robustness audit:** แก้ Gallery no-data `notify(level=...)` crash, visible-range export many-to-many เมื่อ X ซ้ำ, blank Graph จาก invalid workbook plot, residual-plot warning บน X คงที่ และย้าย Charts ให้อยู่ถัดจาก Plot; full suite ผ่านด้วย `-W error`
- **2026-07-06 — Contour, Heatmap:** เพิ่มหมวด Charts/Gallery พร้อม Filled Contour, labeled Contour Lines และ general worksheet Heatmap; ใช้ pure plot module และมี edge tests สำหรับ empty/collinear XYZ
- **2026-07-06 — Multi-Column:** เพิ่ม Stacked Lines by Y Offset + 3D Waterfall, รองรับ registry plots แบบ 3D และแก้ shared placeholder ให้แสดงข้อความบน Axes3D ด้วย `text2D`
- **2026-07-07 — Polar/Frequency/Multi-Panel:** เพิ่ม Polar Line, Polar Scatter, Wind Rose, Phase Plot, Nyquist, Bode และ Subplot Grid; เพิ่ม projection metadata seam, Origin menu categories `Specialized` / `Signal & Frequency` / `Multi-Panel, Multi-Axis`, และ behavioral tests ครอบคลุม projection switching, graph-count +1, add-tab failure, alias-based frequency columns และ multi-panel `canvas.ax`
- **2026-07-07 — Batch export:** เพิ่ม Export → Batch Export Graphs สำหรับเซฟ Graph windows หลายอันในครั้งเดียว, ข้ามกราฟเปล่าโดย default, sanitize ชื่อไฟล์แบบ Windows-safe และ reuse figure export options/print-size behavior เดิม
- **2026-07-07 — Broken axis:** เพิ่ม Plot → Broken Axis สำหรับ active line graph, รองรับ X/Y split axes พร้อม diagonal marks, validation ช่วง break และ tests ทั้ง pure helper + MainWindow flow
- **2026-07-07 — toolbar icon cleanup:** ลด toolbar icon size เป็น 16px, ลด padding ใน `styles/toolbar.qss`, แยก semantic icon key ของปุ่มหลัก/plot bar ไม่ให้ซ้ำกัน และเพิ่ม regression tests สำหรับขนาด + unique icon keys
- **2026-07-07 — Signal transforms:** เพิ่ม signal-processing backends สำหรับ Hilbert, Envelope, Instantaneous Frequency, Auto-correlation, Convolution, Deconvolution, IFFT, STFT และ Zero Padding; pure functions อยู่ใน `analysis/signal_filters.py`, UI เพิ่มคอลัมน์หรือเปิด Book ผลลัพธ์ตามความยาว/รูปทรง output และมี behavioral tests ครอบคลุมทั้งสองชั้น. Current Process UI จัดหมวดเป็น Frequency & Spectrum / Smoothing & Filters / Signal Transforms / Correlation & Convolution
- **2026-07-07 — graph-scoped toolbar binding:** แก้ MDI graph resolver ให้ toolbar actions ใช้ selected/last-selected Graph หลังผู้ใช้โฟกัส Book แทนการย้อนกลับไป Graph 1; ครอบคลุม Plot, overlay, plot toolbar, Crosshair/Box Zoom, Export Figure/Data, Format Graph, Plot from Equation และ Processors/FFT ด้วย user-flow regression tests
- **2026-07-07 — function-menu QAction coverage:** เพิ่ม user-flow tests สำหรับเมนู Process workflow sections, Analysis/Cross-Correlation/Peak Detection, Plot extras, Annotation, Gas Sensor และ Tools/Workflow โดย trigger ผ่าน QAction จริง; baseline ต่อมาหลัง rail polish คือ `616 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — professional activity rail:** รีดีไซน์ rail โมดูลเฉพาะทางจากปุ่มเดี่ยวให้เป็น compact module dock มี header/section/scrollable cards/footer slot รองรับโมดูลอนาคต; เพิ่ม regression test โครง rail และ full suite ตอนนั้น `616 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — parked side panels:** ย้าย Project Explorer, Messages Log และ Smart Hint Log มาอยู่ในแถบ vertical side tabs ซ้ายแบบเก็บข้างได้; เพิ่ม custom tab paint ให้ข้อความไม่โดนตัด, regression test shell/workbook wiring และ full suite ตอนนั้น `617 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — clean sheet-first startup:** ย้าย parked side tabs ไปซ้ายสุดและลด collapsed width เหลือ 24px; MainWindow ใช้ `MdiWorkspace(start_with_graph=False)` เพื่อเปิดด้วย Book1 เท่านั้น, Gas/activity rail ลงทะเบียนแต่ซ่อนจนกว่าจะเรียก `show_activity_context(...)`
- **2026-07-07 — compact useful Settings dialog:** ย่อ Settings เป็น `900x620` พร้อม scroll tab, ตั้ง default theme เป็น Built-in Dark, ทำ Light/Custom QSS ให้ persist จริง, sync Apply-to-Matplotlib font, เพิ่ม Plot Behavior ที่ผูกกับ runtime `plot/mode`, validate QSS/mplstyle path และเพิ่ม regression tests; full suite ล่าสุด `621 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — smarter single-column worksheet plotting:** แก้ `plot_from_workbook()` ให้เลือกคอลัมน์เดียวใด ๆ แล้ว plot ค่าคอลัมน์นั้นเทียบ `Row` 1..N แทนการ fallback ไปคอลัมน์ข้าง ๆ; X/Y designation ใช้กับหลายคอลัมน์หรือไม่มี selection, histogram ใช้ selection ตรง ๆ และ full suite ตอนนั้น `623 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — Plot Details preset/decorations:** แก้ Apply preset ซ้ำแล้วกราฟหดโดยใช้ QWidget canvas size จริงและ restore collapsed single-axes layout; เพิ่ม Excel/Presentation presets, axes shadow/border, legend styling/shadow/rounded และ per-line glow/shadow; full suite ล่าสุด `626 passed, 3 skipped` ด้วย `-W error`
- **2026-07-07 — Origin-level graph decoration + HiDPI shrink fix:** แก้ราก "กราฟย่อเวลาตกแต่ง" — เลิก `set_size_inches` จาก Qt logical pixels (เล็กกว่า widget เท่า devicePixelRatio บนจอ HiDPI) เปลี่ยนเป็น `_relayout_live_figure` = `tight_layout` อย่างเดียว; ขยาย `core/plot_style.py` schema ครบระดับ Origin (typography color/bold/italic/family/pad, ticks length/width/color/rotation/minor/mirror/sci-notation, per-side spines, reference lines แบบ gid ไม่ stack, grid major+minor แยกทุก property + axis, legend title/spacing/markerscale/pads, per-line marker face/edge/fillstyle/drawstyle/zorder) และ dialog เพิ่มแท็บ Ticks && Spines / Reference Lines + control ครบทุก key; full suite ล่าสุด `637 passed, 3 skipped`
- **2026-07-07 — Charts data mapping + axis tick labels:** เพิ่ม `PlotDataMappingDialog` ก่อน registry/basic advanced charts เพื่อเลือก Primary/X, Y series, Z และ Group แล้ว plot จาก DataFrame ชั่วคราวที่ reorder แล้ว (Cancel ไม่สร้าง Graph); ขยาย Plot Details ด้วยแท็บ Tick Labels แบบ Origin (decimal/scientific/engineering/percent, divide factor, prefix/suffix, plus/minus, thousands) และ Reference Lines label/width/opacity; full suite ล่าสุด `644 passed, 3 skipped` ด้วย `-W error`
- **2026-07-08 — Data workflow menu redesign:** แทน Data menu เก่า 2 action ด้วย workflow menu แยก `Active Book` / `Columns` / `Units + Metadata` / `Quick Transforms` / `Clean Data`, คง `Ctrl+Shift+D` ของ Derived Column และเพิ่ม user-flow tests ที่ trigger QAction จริง; full suite ล่าสุดหลัง toolbar redesign คือ `670 passed, 3 skipped` ด้วย `-W error`
- **2026-07-08 — two-row Origin function toolbar:** แทน toolbar แถวเดียว/plot bar ล่างด้วย function bar สองแถวด้านบน, รวม File/Data/Plot/Process/Analysis/Annotation/Gas/Workflow/Export/View actions หลักไว้ใน `toolbar_actions`, ใช้ Material icon key ไม่ซ้ำ และเพิ่ม toolbar user-flow tests ที่ trigger ปุ่มจริงในหมวด Process/Analysis/Annotation/Gas/Workflow; full suite ล่าสุด `670 passed, 3 skipped` ด้วย `-W error`
- **2026-07-08 — Origin-like Analysis menu categories:** เพิ่มหมวด `Statistics` / `Mathematics` / `Data Manipulation` / `Fitting` / `Signal Processing` / `Peaks and Baseline` ในเมนู Analysis ตาม Origin, mirror ฟังก์ชันที่มีจริงเข้า Analysis, เพิ่ม `Decimation...` เป็น signal transform ที่เปิด result Book และเพิ่ม QAction tests จากทั้ง Analysis menu + two-row toolbar; full suite ล่าสุด `673 passed, 3 skipped` ด้วย `-W error`
- **2026-07-08 — Analysis menu UX rework (result Books):** ยกเครื่อง Descriptive Statistics / Covariance Matrix / Peak Metrics / Signal Quality จาก UI ยุคก่อนรื้อ (อ่าน `cbY` ซ่อน + ผลลัพธ์เป็น text ใน message box) เป็นฟอร์มเดียวที่เลือกคอลัมน์เห็น ๆ + โชว์ชื่อ Book แหล่งข้อมูล (`_active_book_label`) → เปิดผลเป็น **result Book** แบบ Origin result sheet ผ่าน `_open_signal_result_book`; เพิ่ม pure helpers `descriptive_table`/`covariance_table` (แถม Correlation), `peak_metrics_summary`/`signal_quality_summary`, Peak Metrics ได้ peak_x/height เพิ่มและรองรับ All-numeric (ตัดคอลัมน์ X อัตโนมัติ), Signal Quality ถาม fs ในฟอร์มเดียว, `_finite_xy_for_metrics` fallback เป็น row index เมื่อ X เป็น text และอัปเดตข้อความ `_has_y_data` ที่อ้างปุ่ม UI เก่า; full suite ล่าสุด `653 passed, 3 skipped`
- **2026-07-08 — Origin Scale tab + tick-label Formula:** เพิ่มแท็บ `Scale` ใน Plot Details (From/To/Type/Reverse ต่อแกน, Rescale margin %, Major ticks "By Increment" + Anchor Tick, Minor ticks "By Increment"/"By Counts") และช่อง `Formula` ในแท็บ Tick Labels (เช่น `2*x` — ชนะ Divide by Factor ตามกติกา Origin); engine เพิ่ม `compile_tick_formula` (AST whitelist ปลอดภัย ไม่รัน arbitrary code), `MultipleLocator(offset)` สำหรับ anchor, `AutoMinorLocator` สำหรับ by-counts และ `ax.margins` สำหรับ rescale margin; ย้ายกลุ่ม Range & Scale จากแท็บ Axes ไปแท็บ Scale; full suite ล่าสุด `660 passed, 3 skipped`
- **2026-07-08 — decoration diff-apply stability:** แก้ราก "แตะสีตัวหนังสือแล้วกราฟหาย/เพี้ยน" — เดิม Apply ส่งค่าทุก control กลับไปทับกราฟทั้งใบ (identity Apply ยังเลื่อน margin เอง และ seed ที่อ่านเพี้ยนตัวเดียวทำกราฟพังได้); ตอนนี้ dialog เก็บ baseline ตอนสร้าง + `_apply_plot_details` apply เฉพาะ `diff_style(baseline, current)` + re-baseline หลัง apply (revert-after-apply ทำงาน); กัน `fontsize=None` เมื่อ key มาเดี่ยว, ปิด tick-label override แล้วคืน ScalarFormatter เฉพาะแกนที่ tag `_ps_tick_label_fmt`; เพิ่ม contract tests: identity Apply = visual no-op, เปลี่ยนสี title แตะแค่ title, revert หลัง apply; full suite ล่าสุด `666 passed, 3 skipped`
- **2026-07-08 — core modules + English UI pass:** added active-book dataset operations (duplicate/rename/group/merge/split/filter/search), cleaning operations (remove missing rows, crop range, merge by timestamp), harmonic analysis, workflow auto report/snapshot/compare/audit, and an English UI normalization seam for prompts/dialog chrome. Fixed the QApplication English event filter so it installs once instead of accumulating per MainWindow instance; full suite latest `684 passed, 8 skipped` with `-W error`.
- **2026-07-08 — startup/test-suite optimization:** Settings Apply now diff-applies appearance/Matplotlib work so plot-mode-only changes do not repaint the whole QApplication; test harness closes leftover Qt top-level widgets between tests; MainWindow startup caches qtawesome/QIcon assets and shell QSS, and debug startup prints moved to logging. Full suite latest `684 passed, 8 skipped` in `228.27s / 0:03:48` with `-W error`.
- **2026-07-08 — Process menu workflow redesign:** replaced the flat Process dropdown with grouped workflow sections (`Quick Actions`, `Frequency & Spectrum`, `Smoothing & Filters`, `Signal Transforms`, `Correlation & Convolution`, `Clean & Prepare Data`, `Summarize & Aggregate`), keeping all backend QAction paths covered by menu-flow tests. Full suite latest `687 passed, 8 skipped` in `224.36s / 0:03:44` with `-W error`.
- **2026-07-08 — Electrochemistry module first pass:** added `analysis/electrochemistry.py`, `main_window_electrochemistry_mixin.py`, and `UI/electrochemistry_panel.py`; module registers in the activity rail without opening on startup and adds an `Electrochemistry` menu with CV peak metrics, Randles-Sevcik/ECSA, Tafel, GCD supercapacitor metrics, and EIS Rs/Rct/Nyquist/Bode flows. Result tables open as Books and plots open as Graphs. Focused module/menu/rail suite: `98 passed` with `-W error`.
- **2026-07-09 — Spectroscopy module first pass:** added `analysis/spectroscopy.py`, `main_window_spectroscopy_mixin.py`, and `UI/spectroscopy_panel.py`; module registers under the shared Modules Gallery and `Modules -> Spectroscopy` menu with baseline correction/normalization, peak table, Raman D/G ratio, Tauc band gap, and XRD Scherrer size. Result tables open as Books and spectroscopy plots open as Graphs. Full suite: `715 passed, 3 skipped` with `-W error`.
- **2026-07-09 — Materials Science module first pass:** added `analysis/materials.py`, `main_window_materials_mixin.py`, and `UI/materials_panel.py`; module registers under the shared Modules Gallery and `Modules -> Materials Science` menu with I-V conductivity/resistivity/sheet resistance, Arrhenius activation energy, TGA/DSC thermal metrics, and sample ranking. Result tables open as Books and analysis plots open as Graphs. Full suite: `721 passed, 3 skipped` with `-W error`.
- **2026-07-09 — Physics / General Lab module first pass:** added `analysis/physics_lab.py`, `main_window_physics_mixin.py`, and `UI/physics_panel.py`; module registers under the shared Modules Gallery and `Modules -> Physics / General Lab` menu with Ohm's law fit, RC time constant, pendulum gravity fit, and power-product uncertainty propagation. Result tables open as Books and lab-fit plots open as Graphs. Full suite: `727 passed, 3 skipped` with `-W error`.
- **2026-07-10 — QA pass + workflow re-run fix:** production-style exploratory QA over gallery (45 plots × 6 edge datasets), Plot Details lifecycle (identity/preset/revert stability), multi-book state, session/project round-trips, scale/formula, analysis result Books, and figure/batch export — all clean. Found + fixed one real bug: **`wf_import_and_run` used a strict `replay()`**, so a workflow that recorded an analysis-only op (Descriptive Statistics / Covariance / Peak Metrics / Signal Quality / `dataset_*` / several signal transforms — no replay entry) aborted the whole re-run with "Unknown operation". Re-run is now lenient (skips non-transform ops like the generated script already does) and reports which ops it skipped; regression test added. Full suite `732 passed, 3 skipped`.
- **2026-07-10 — Reset View no longer hides the graph:** right-click → zoom → Reset View made the plot vanish. Two causes: (1) `context_menu.py::_on_reset_view` restored a stored `_zoom_hist[0]` snapshot captured while the axes was still empty (limits `(0,1)`) — resetting pushed the data off-screen; (2) `main_window_view_mixin._reset_view` used `set_xlim(auto=True)` + `draw()`, which only flips the autoscale flag and never recomputes the view, so the graph stayed stuck at the zoomed limits. Both now autoscale from the axes' `dataLim` (`autoscale(enable=True)` + `autoscale_view()`) so every artist (lines and scatter/collections) comes back into view; context reset also re-baselines `_zoom_hist[0]` to the true data view. New `tests/test_context_menu.py` + updated view-mixin test.
- **2026-07-11 — customizable theme system (accent + background + light/dark):** rebuilt `styles/theme.py` into one runtime — `build_theme_palette(mode, accent, background)` derives the full palette (surface/border/text/muted + WCAG contrast + auto light/dark from any background color, incl. pure black), `apply_theme_from_config()` is the single entry for startup and Settings, and `_ApplicationThemeRuntime` (app-level event filter) retints component QSS (`render_theme_qss`), rebases QPalette/font on late-created dialogs/popups, and calls each widget's `apply_application_theme()` hook (MdiWorkspace viewport brush, WorkbookWidget meta brushes). Icons go monochrome-themed through `themed_icon()`/`refresh_application_icons()` with `preserveIconColors` opt-out for colored thumbnails/ColorButton. Settings dialog gains Accent Color + Background pickers (presets + custom) and a Matplotlib font-size spin; `AppearanceConfig` adds `theme_mode`/`accent_color`/`background_color` with legacy-qss migration, and `main()` forces non-native dialogs/menus so the theme covers every popup. Relocated the specialized tool groups off the two-row top bar into categorized Left/Right/Bottom docks (`build_side_toolbars` + `_create_dock_tool_groups`) keeping every `toolbar_actions` key. Full suite `760 passed, 3 skipped`.
